
export function ChangePasswordModal({ close, refresh }: { close: () => void, refresh: () => void }) {
    return (
        <div className="fixed">
            <h1>Change Password Modal</h1>
        </div>
    )
}