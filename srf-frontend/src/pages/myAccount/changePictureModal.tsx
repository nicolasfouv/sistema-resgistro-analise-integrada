
export function ChangePictureModal({ close, refresh }: { close: () => void, refresh: () => void }) {
    return (
        <div className="fixed">
            <h1>Change Picture Modal</h1>
        </div>
    )
}